{
  "name": "retroroms",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first ROM library curator and portable ES-DE export manager",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "package:mac": "node apps/desktop/package-macos.mjs",
    "package:mac-native": "node apps/macos-native/package-macos-native.mjs",
    "package:linux": "node apps/desktop/package-platform.mjs linux",
    "package:windows": "node apps/desktop/package-platform.mjs windows"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED"
}
