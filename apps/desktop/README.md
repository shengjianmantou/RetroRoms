# Desktop application

The macOS MVP will package the local API, browser UI, archive handlers, and catalog migrations into a signed universal application. Application data remains beside the processed library rather than inside the application bundle.

