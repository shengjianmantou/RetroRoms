# Development CLI

Exercises the ingestion pipeline before the desktop UI is available. It creates or updates `.rom-curator/catalog.sqlite` and `.rom-curator/manifest.json` in the selected processed library. It does not export ROMs.

